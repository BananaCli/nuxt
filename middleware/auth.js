export default function({route,store,redirect}){
    // const islogin = store.state.user.token
    // if(route.name == "login" && islogin)
    //     redirect("/")
    // if((route.name == "dashboard-index" || route.name == "shopcar") && !islogin)
    //     redirect("/")
}